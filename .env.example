# Shopify Storefront API (PUBLIC token — safe to ship to the client).
# Do NOT commit a real .env file. Set these on Render → Environment.
NEXT_PUBLIC_SHOPIFY_DOMAIN=mellospellmans-clothing.myshopify.com
NEXT_PUBLIC_SHOPIFY_STOREFRONT_TOKEN=your_public_storefront_token_here
